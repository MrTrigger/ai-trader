[package]
name = "records"
version = "0.1.0"
edition = "2021"

[dependencies]
sqlx = { version = "0.8", default-features = false, features = ["runtime-tokio", "tls-rustls", "postgres", "time", "migrate", "macros"] }
thiserror = "1"
time = { version = "0.3", features = ["parsing", "formatting"] }
